services:
  fila2pro:
    build: .
    container_name: fila2pro-web
    ports:
      - "8080:8080"
    volumes:
      - fila2pro_data:/app/data   # SQLite persisté ici
    restart: unless-stopped

volumes:
  fila2pro_data:
